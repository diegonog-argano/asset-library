# Copy to .env and fill in values. .env is gitignored.
# Generate a token at https://www.figma.com/settings (Personal access tokens).
# Token only needs file_content:read scope.
FIGMA_TOKEN=
FIGMA_FILE_KEY=
